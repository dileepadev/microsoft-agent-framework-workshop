# client/web — Vite + React client

> [!NOTE]
> Placeholder. Built in Phase 4 — see [TODO.md](../../TODO.md).

A Vite + React chat UI for OpsAgent, as its own npm project, deployable as static
files to GitHub Pages or Vercel. Talks to `app/api.py` over HTTP.
