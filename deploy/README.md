# deploy — hosting the agent

> [!NOTE]
> Placeholder. Built in Phase 5 — see [TODO.md](../TODO.md).

Participants pick a host the same way they pick a provider. Planned:
`render.yaml`, a Dockerfile, and `azure/` templates, with guides for Render,
FastAPI Cloud, Azure Container Apps, Azure Functions and Foundry Hosted Agents.

Plain files, no environment of its own.

> [!IMPORTANT]
> Environment variables are documented per host. Never commit a `.env`.
